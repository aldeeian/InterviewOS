# Copy to .env.local and fill in to enable real AI features.
# Without either set, /api/copilot/suggest automatically falls back to the
# local heuristic engine in lib/copilot.ts — no key required to run the app.
# If both are set, Gemini takes priority.
GEMINI_API_KEY=
ANTHROPIC_API_KEY=
